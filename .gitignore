node_modules
.local/
dist
.expo
coverage
.DS_Store

# Cursor agent debug NDJSON (session-specific; do not commit)
.cursor/debug-*.log

# App Store Connect API key — never commit
apps/mobile/credentials/*.p8

# Google Play service account key — never commit
apps/mobile/credentials/*.json

# Supabase (local only; do not commit URL or key)
apps/mobile/.env
